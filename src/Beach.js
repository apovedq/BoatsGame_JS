/* class Beach {
    

    showObstacle(img, x, y, width, height) {
        this.x = x;
        this.y = y;
        image(img, this.x, this.y,width,height);
    }

    getX() {
        return this.x;
    }
    
    getY() {
        return this.y;
    }
} */